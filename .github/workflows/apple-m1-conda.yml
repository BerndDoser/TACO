name: Conda environment on Apple M1

on:
  push:
    branches:
    - main

permissions:
  contents: read

jobs:
  deploy:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: '3.10'
